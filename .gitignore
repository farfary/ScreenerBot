target/*
crates/*
**/.DS_Store

# Log files
logs/*

# Data directory (contains all bot-generated files)
data/

# Legacy cache files (now in data/cache_*)
.pool_cache.json
.solana_pairs_cache.json
.token_decimals_cache.json
.tokens_cache.json
.blacklist.json
.cache_ohlcv/*
.cache_trades/*
.cache_ohlcvs/*

# Build artifacts
Cargo.lock

# Large database files (anywhere in project)
*.db
*.sqlite
*.sqlite3

# Large cache files
*cache*.json
.dexscreener_cache.json
cache_tokens.db

# Log files (anywhere in project)
*.log
logs/

# Backup files
*_backup.*
*.backup

# IDE/Editor folders
.vscode/
.idea/

# Development tools (keep local, not tracked)
.playwright-mcp/
dist/
analysis-exports/
public/

# Dependencies
node_modules/
.venv/

# Build artifacts and temporary files
builds/
.build.lock
_trash/
_setup/*

# Private development files (not part of source)
build.sh
build-windows.bat
build-cleanup.sh
run.sh
run.bat
run-dev.bat
publish.sh
/scripts/
vless/
control-panel/
_public-legacy/
Bot/
_backup/

# Agent instructions (private)
.claude/
.github/*
!.github/workflows/
.folderinfo
# Agent instruction files — stay on disk but are not part of the public repo
**/AGENTS.md

# Architecture docs — kept on disk, not part of the public repo
/docs/

# Lint/format configs (development only)
.htmlvalidate.json
.prettierrc.json
.stylelintrc.json
eslint.config.js

