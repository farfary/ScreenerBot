- rename src/webserver/routes/filtering_api.rs to src/webserver/routes/filter.rs
- ✅ ~~find and remove all truncated token mint address and pool address and tx signature in logs and events and codes~~ (DONE: 20 truncations removed from utils.rs + raydium_cpmm.rs)
- add trader tab to dashboard
- add stats tab to dashboard ( for each module and services like apis calls count success and failure, latency, etc)
- add coverage checks for home dashboard trader analytics (period ranges, dca, partial exits)

- in vps install script manager if have update for script we need show it in menu of script, also add it to Assistant instructions always doing increase schema or verion if needed for some special things like this or in main bot, script is in public repo

- add to Assistant instructions always for relase note of new version publishing we need check git history and do make a release not for users, not doing say anything about code level or development tools for project, we need only say about improve or fixes or features of website and main bot or public repo updating, for website only doing say about we added a doc to documents
